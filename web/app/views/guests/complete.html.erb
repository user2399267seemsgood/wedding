<div class="container">
  <div class="row">
    <div class="offset-lg-2 col-lg-8">
      <%= render partial: 'leader' %>
      <h2><%= t :form_thanks %></h2>
      <p><%= t :form_we_sent_an_email %><tt><%= @guest.email %></tt>.</p>
      <p><%= t :form_check_your_email_few_minutes %><%= mail_to ENV['CONTACT_EMAIL'], (t :form_contact_us) %>.</p>
      <p><%= t :form_email_contains_rsvp_info %></p>
      <p><%= link_to (t :form_back_to_homepage), root_path %></p>
    </div>
  </div>
</div>
