<div class="container">
  <div class="row">
    <div class="offset-lg-2 col-lg-8">
      <%= render partial: 'leader' %>
      <h2><%= t :form_welcome_back_title %></h2>
      <p><%= t :form_welcome_back_text %></p>
      <p><%= t :form_check_your_email %><tt><%= @guest.email %></tt>.</p>
      <p><%= t :form_email_not_received %><%= mail_to ENV['CONTACT_EMAIL'], (t :form_contact_us) %>.</p>
    </div>
  </div>
</div>
